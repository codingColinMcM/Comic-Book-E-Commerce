import React from 'react';

export default function Sell() {
  return (
    <div>
      
    </div>
    //needs to authenticate user is logged in
    //should be a form with fields for title, issue, volume, condition, price, ???
    //need submit button
    //should maybe display success message and return to either newly listed or user profile
  );
}
